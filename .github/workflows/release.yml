name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build all platforms
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build UI
        run: bun run --cwd packages/orchestrator-ui build

      - name: Prepare orchestrator build
        run: |
          mkdir -p apps/orchestrator/dist
          cp packages/orchestrator-ui/dist/index.html apps/orchestrator/dist/ui.html

      - name: Build binaries (cross-compile)
        run: |
          cd apps/orchestrator

          # Build for each target using Bun's cross-compilation
          bun build src/index.ts --compile --target=bun-darwin-arm64 --outfile dist/claude-orchestrator-macos-arm64 --minify
          bun build src/index.ts --compile --target=bun-darwin-x64 --outfile dist/claude-orchestrator-macos-x64 --minify
          bun build src/index.ts --compile --target=bun-linux-x64 --outfile dist/claude-orchestrator-linux-x64 --minify
          bun build src/index.ts --compile --target=bun-linux-arm64 --outfile dist/claude-orchestrator-linux-arm64 --minify
          bun build src/index.ts --compile --target=bun-windows-x64 --outfile dist/claude-orchestrator-windows-x64.exe --minify

      - name: Create archives
        run: |
          cd apps/orchestrator/dist

          # Unix archives
          tar -czvf claude-orchestrator-macos-arm64.tar.gz claude-orchestrator-macos-arm64
          tar -czvf claude-orchestrator-macos-x64.tar.gz claude-orchestrator-macos-x64
          tar -czvf claude-orchestrator-linux-x64.tar.gz claude-orchestrator-linux-x64
          tar -czvf claude-orchestrator-linux-arm64.tar.gz claude-orchestrator-linux-arm64

          # Windows archive
          zip claude-orchestrator-windows-x64.zip claude-orchestrator-windows-x64.exe

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-binaries
          path: |
            apps/orchestrator/dist/*.tar.gz
            apps/orchestrator/dist/*.zip

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-binaries
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
          generate_release_notes: true
